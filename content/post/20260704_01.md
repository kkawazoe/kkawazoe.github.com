---
title: "Mac 版 Obsidian Vault を Windows で開く方法"
slug: "how-to-open-the-mac-version-of-obsidian-vault-on-windows"
tags: [ "Tools", "Obsidian" ]
thumbnail: "images/logo/obsidian_logo.svg"
description: "Mac 版 Obsidian Vault を Windows で開く方法を備忘録として残しておく"
mathjax: false
mermaid: false
iframe: false
date: 2026-07-04T00:00:00+09:00
draft: false
type: "post"
---

Mac 版 Obsidian Vault を Windows で開く方法を備忘録として残しておく

## 環境

- OS: Windows 11 Pro
- WSL: WSL2
  - OS: Ubuntu 24.04

## 方法

WSL 内に Linux 版 Obsidian をインストールする

1. systemd有効化

    ```bash
    sudo nano /etc/wsl.conf
    ```

    以下を追加：

    ```bash
    [boot]
    systemd=true
    ```

    WSLを再起動（PowerShellで実行）：

    ```bash
    wsl --shutdown
    ```

2. Flatpak導入

    ```bash
    sudo apt update && sudo apt upgrade -y
    sudo apt install -y flatpak
    sudo flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
    ```

3. Obsidianインストール

    ```bash
    sudo flatpak install -y flathub md.obsidian.Obsidian
    ```

4. 起動

    ```bash
    flatpak run md.obsidian.Obsidian
    ```
